node_modules/
__pycache__/
.DS_Store
runs/
screenshots/
public-bot/
public-client/

server.json
.env

backups/
.obsidian/

# Bot folders (except template, shared trainer, and progressive entry)
bots/*/
!bots/_template/
!bots/_shared/
!bots/progressive/
bots/**/bot.env
bots/**/trainer-memory.json
bots/**/trainer-run.*
logs/

# Daemon type checker temp file
sdk/__exec_check.ts

# Upstream npm lockfile; this repo builds the engine with bun/bun.lock only.
# Deleted locally — ignore so vendor syncs don't silently reintroduce it.
server/engine/package-lock.json

# SDK is copied into Docker via Dockerfile; don't duplicate it under server/
server/sdk/
jobs/